import React from "react";
import "./style.scss";

const Title = () => {
    return (
        <div className="title-section">
            <h1>Connexion admin</h1>
        </div>

    );
};

export default Title;